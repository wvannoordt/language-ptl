#define myvar 4
#define myvartwo -2.7899e-7//This is a tricky comment to handle
#define myExpandedVariable @sqrt(7.65, $(myvar))
GlobalSettings
{
	Display
	{
		debugLevel = $(myvar) // this is an inline comment
		debugLevelzap = 3
	}
	Memory_1
	{
		allowStackAllocation = false
	}
}
Domain
{
	blockDim = [1, 2, 2]
	coolio = @sqrt($(myvartwo))  // this is another inline comment
}
some_global_definition = myStringIdentifier
//parallel